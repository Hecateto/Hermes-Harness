## Claude Code Design
--- 

### 1. 关于压缩 （bottom-up）
- 大结果存磁盘 
- Snip远古信息（插入清理标记）
- 时间衰减；清空可重新获取的工具结果，保留不可重复的结果（subagent状态，task状态等）
- 保留对话历史，读时投影压缩视图
- 全量摘要，阈值触发

--- 


### 2. 关于Auto-Compact
---
#### 关于触发
- 阈值是什么？
按照有效上下文窗口➖固定值，根据摘要任务的大规模数据统计得出
- 自动/手动
共享压缩函数，手动能指定压缩关注点
- 守卫
防止多次压缩失败重试导致的token爆炸；防止subagent递归调用压缩
---
#### 关于压缩
- 全量重写
不是压缩前文➕保留最近的，而是全量压缩
最近对话可恢复，包括附件（读过文件，计划文件，skill，任务状态， etc.），hook结果
**语义信息走摘要，状态信息走附件**
- 恢复策略
阈值：文件数/文件token数/token预算
- microcompact
保留工具指令，清空工具输出
- 多流处理
CLAUDE.md，system prompt 不进压缩，异步任务状态作为附件保留/重注入

---
#### 关于摘要prompt的设计
禁止工具调用（仅文字）
输出格式化：
1. \<analysis\>（最终删除）
2. \<summary\>（保留）
    - 请求和意图
    - 关键技术概念
    - 文件和代码
    - 错误和修复
    - 解决的问题
    - **用户信息（枚举所有用户消息，除了toolresult）**
    - 代办任务
    - **当前任务（最细粒度，方便压缩后接续任务）**
    - 下一步建议
---
#### 关于摘要模型
使用对话同模型：保证质量，复用prompt cache

---
#### 关于接续对话
摘要器压缩➕清空缓存➕并发生成附件➕组装新消息链并替换，过程透明
旧消息（完整对话记录）写入本地 **transcript** 文件
摘要开头prompt: 明确接续对话场景 + 摘要正文 + 可查**transcript**提醒
